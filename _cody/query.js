
const userPref = //Selection of tag content for users pref turned into a string

const itemsFromInventory = //Selection of items not belonging to logged in user

const tagsPerItem = {

  item: {
    'tagString': tagString
  }

}

itemsFromInventory.forEach((item) => {

  const tagsForItem = //Selection for ${item} tags (pushing into tagsPerItem)

})

for(let item in tagsPerItem) {

  //Run string comparison function (tsvector?)

}
